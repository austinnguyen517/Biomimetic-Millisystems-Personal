
"use strict";

let robotData = require('./robotData.js');
let envData = require('./envData.js');
let mapData = require('./mapData.js');

module.exports = {
  robotData: robotData,
  envData: envData,
  mapData: mapData,
};
